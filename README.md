RDX 1.0 is a basic C++ script designed to be ran on Windows 11.
It does the following:
Logs keystrokes, saves to a folder called /logs every hour
Searches /logs and sends the keylogs to a python flask server
Establishes reverse shell 
